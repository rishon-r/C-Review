#include <stdio.h>

int main(){

  // All arithmetic operators +, -, /, * exist in C
  // Similar to Python, the +=, -=, /=, *= augmented assignment operators also exist in C
  // C also has to different augmented assignment operators: ++, and --, used as follows: x++, x--, in order to increment or decrement
  // the value of a variable by one respectively

  // = operator is the assignment operator
  // == is used to check for equality


  return 0;

}